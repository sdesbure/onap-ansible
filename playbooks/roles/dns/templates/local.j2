//
// Do any local configuration here
//

// Consider adding the 1918 zones here, if they are not used in your
// organization
//include "/etc/bind/zones.rfc1918";

{% for zone in zones %}
zone "{{ zone }}" {
    type master;
    file "/etc/bind/zones/db.{{ zone }}"; # zone file path
};

{% endfor %}